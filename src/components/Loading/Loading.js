import React from 'react';
// import { Spinner } from 'react-bootstrap';

const Loading = () => {
    return (
        <div>
            <p>Loading</p>
        </div>
    );
};

export default Loading;